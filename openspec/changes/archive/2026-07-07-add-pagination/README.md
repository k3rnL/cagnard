# add-pagination

Add pagination to Cagnard where unbounded lists can become slow or unusable. This change should cover provider-neutral storage browsing first, then task detail lists and any other large collection surfaces that need incremental loading, while preserving current filtering, sorting, selection, transfer, and provider capability semantics.
