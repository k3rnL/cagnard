# add-runnable-examples

Plan runnable examples that let new users start Cagnard from scratch, including simple local storage, static users, S3-compatible storage, MinIO, docker-compose, Helm values, and relevant provider/auth combinations.
